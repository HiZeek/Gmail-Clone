import React from 'react';
import "../styles/EmailRow.css";

const EmailRow = ({ id, title, subject, description, time }) => {
  return (
    <div className='emailRow'>EmailRow</div>
  );
};

export default EmailRow;